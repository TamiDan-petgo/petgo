/**
 * Enum containing error codes for Firebase Auth
 */
export enum EnumFirebaseAuthError {
    EmailAlreadyInUse = "auth/email-already-in-use",
    InvalidEmail = "auth/invalid-email",
    OperatioNotAllowes = "auth/operation-not-allowed", 
    WeakPassword = "auth/weak-password"
}